import { LinesChecklist } from "./LinesChecklist";

// 現時点ではLinesChecklistコンポーネントを表示するだけ
export default function LinesPage() {
    return (
        <main className="p-6">
            <LinesChecklist />
        </main>
    );
}